# Portable workshop overlay. `dshx overlay` / `verify` rewrite `name` to an
# absolute path. Official installs use cordis.patch.yml (package name).
- insert:
    - id: llm-antigravity-oauth
      name: './src/index.ts'
